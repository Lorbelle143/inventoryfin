{
  "name": "inventory-financial-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"api,web\" --prefix-colors \"cyan,magenta\" \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "ts-node-dev --respawn --transpile-only src/index.ts",
    "dev:web": "npm run dev --prefix frontend",
    "build": "tsc",
    "build:vercel": "npx prisma generate && npx tsc --noEmit",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.19.0",
    "@vercel/node": "^5.8.9",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.4",
    "express-rate-limit": "^8.5.2",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.19.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.18",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^20.14.2",
    "concurrently": "^10.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2"
  }
}
